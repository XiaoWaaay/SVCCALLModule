App 选择监控目标
GUI 展示已安装应用列表，勾选即可开始监控
PID 自动跟踪
目标 App 启动/重启后自动捕获新 PID
全部 syscall 监控
通过 sys_call_table hook 拦截所有系统调用
参数深度解析
文件路径、IP 地址端口、ioctl cmd、mmap 参数等人类可读格式
调用来源 .so 识别
通过 LR (x30) 寄存器 + /proc/pid/maps 解析发起调用的库名
用户态调用栈回溯
通过 FP (x29) 链尝试回溯多级调用栈
反调试行为捕获
高亮显示 ptrace、/proc/self/status 读取等反调试行为
Syscall 过滤
只监控感兴趣的 syscall 类别（文件/网络/内存/进程）
日志持久化
自动保存到设备存储，支持导出分析